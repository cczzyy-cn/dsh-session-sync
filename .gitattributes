# Normalise source line endings to LF in the repository and on checkout, so a
# clone on any platform matches what was committed.
* text=auto eol=lf

# Committed build output must stay byte-identical to what the build produced:
# the bundles are the artifacts the client module registry serves, and CRLF
# conversion would break them.
lib/** -text
client/** -text
